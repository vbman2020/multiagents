/**
 * Main entry point for the multiagents library
 */

export { clamp, lerp, roundTo } from './utils/math-utils';
export { parseXml, stringifyXml, validateXml, XmlElement, ValidationResult } from './utils/xml-utils';
